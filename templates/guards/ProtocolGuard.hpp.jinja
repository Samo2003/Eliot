/**
 * @file {{ guard.cpp_type() }}.hpp
 * @brief Generated guard.
 *
 * Generated by Eliot.
 *
 * Guard: {{ guard.cpp_type() }}
 * Purpose: Activation based on protocol identifier.
 *
 * This file was automatically generated.
 */

#ifndef {{ guard.hpp_define() }}
#define {{ guard.hpp_define() }}

#include "../Packet.hpp"

namespace eliot_generated {

    /**
     * @brief Stateless protocol-based guard condition.
     *
     * This guard is fulfilled only for packets whose protocol
     * (IPv4 Protocol field or IPv6 Next Header field)
     * matches the configured identifier.
     */
    struct {{ guard.cpp_type() }} {

        /**
         * @brief Evaluates whether the guard condition is fulfilled.
         *
         * The guard queries the protocol field at the configured
         * network-layer position and compares it against
         * the expected protocol identifier.
         *
         * @param packet Pointer to the evaluated packet.
         * @return true if the packet uses protocol '{{ guard.id }}'.
         */
        static inline bool fulfilled(const Packet* packet) noexcept {
            return packet->protocol({{ guard.get_nh() }}) == {{ guard.id }};
        }
    };
}

#endif
