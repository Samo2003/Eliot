/**
 * @file {{ guard.cpp_type() }}.hpp
 * @brief Generated guard.
 *
 * Generated by Eliot.
 *
 * Guard: {{ guard.cpp_type() }}
 * Purpose: Activation based on packet size comparison.
 *
 * This file was automatically generated.
 */

#ifndef {{ guard.hpp_define() }}
#define {{ guard.hpp_define() }}

#include "../Packet.hpp"
{% if not guard.not_generator_size() %}
#include "../static/interfaces/GeneratorInt.hpp"
#include "../generators/{{ guard.size.cpp_type() }}.hpp"
{% endif %}

namespace eliot_generated {

    /**
     * @brief Packet size-based guard condition.
     *
     * This guard is fulfilled when the packet size
     * satisfies the configured comparison condition.
     */
    struct {{ guard.cpp_type() }} {

        /**
         * @brief Evaluates whether the guard condition is fulfilled.
         *
         * Compares packet size against the configured value
         * using operator '{{ guard.get_op() }}'.
         *
         * @param packet Pointer to the evaluated packet.
         * @return true if the size comparison condition is satisfied.
         */
        {{ 'static ' if guard.not_generator_size() or not guard.size.is_state() else '' }}inline bool fulfilled(const Packet* packet) noexcept {
            {% if guard.not_generator_size() %}
            return packet->size() {{ guard.get_op() }} {{ guard.size }};
            {% else %}
            return packet->size() {{ guard.get_op() }} generate{{ guard.size.cpp_call() }});
            {% endif %}
        }

        {% if not guard.not_generator_size() and guard.size.is_state() %}
        private:

            ///> Generator providing dynamic size threshold values.
            {{ guard.size.cpp_type() }} {{ guard.size.cpp_name() }};
        {% endif %}
    };

}

#endif
