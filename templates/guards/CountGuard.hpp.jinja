/**
 * @file {{ guard.cpp_type() }}.hpp
 * @brief Generated guard.
 *
 * Generated by Eliot.
 *
 * Guard: {{ guard.cpp_type() }}
 * Purpose: Activation based on evaluation count.
 *
 * This file was automatically generated.
 */

#ifndef {{ guard.hpp_define() }}
#define {{ guard.hpp_define() }}

#include "../Packet.hpp"

namespace eliot_generated {

    /**
     * @brief Counter-based guard condition.
     *
     * This guard becomes fulfilled after a specified number of
     * packet evaluations. 
    {% if guard.duration is not none %}
     * It remains active only for a limited duration (number of subsequent evaluations).
    {% endif %} 
     */
    struct {{ guard.cpp_type() }} {

        /**
         * @brief Evaluates whether the guard condition is fulfilled.
         *
         * The guard activates after {{ guard.after }} packet evaluations.
        {% if guard.duration is not none %}
         * The guard remains active only for {{ guard.duration }} subsequent evaluations.
        {% endif %}
         *
         * @param packet Pointer to the evaluated packet (unused).
         * @return true if the guard condition is satisfied.
         */
        inline bool fulfilled(const Packet* /*packet*/) noexcept {
            {% if guard.duration is none %}
            return ++_counter > {{ guard.after }};
            {% else %}
            return  ++_counter > {{ guard.after }} && _counter <= {{ guard.after + guard.duration }};
            {% endif %}
        }

        private:

            ///> Packet evaluation counter.
            size_t _counter = 0;
    };
}

#endif
