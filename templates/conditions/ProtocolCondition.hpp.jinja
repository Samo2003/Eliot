/**
 * @file {{ condition.cpp_type() }}.hpp
 * @brief Generated condition.
 *
 * Generated by Eliot.
 *
 * Condition: {{ condition.cpp_type() }}
 * Purpose: Activation based on protocol identifier.
 *
 * This file was automatically generated.
 */

#ifndef {{ condition.hpp_define() }}
#define {{ condition.hpp_define() }}

#include "../Packet.hpp"

namespace eliot_generated {

    /**
     * @brief Stateless protocol-based condition condition.
     *
     * This condition is fulfilled only for packets whose protocol
     * (IPv4 Protocol field or IPv6 Next Header field)
     * matches the configured identifier.
     */
    struct {{ condition.cpp_type() }} {

        /**
         * @brief Evaluates whether the condition condition is fulfilled.
         *
         * The condition queries the protocol field at the configured
         * network-layer position and compares it against
         * the expected protocol identifier.
         *
         * @param packet Pointer to the evaluated packet.
         * @return true if the packet uses protocol '{{ condition.id }}'.
         */
        static inline bool fulfilled(const Packet* packet) noexcept {
            return packet->protocol({{ condition.get_nh() }}) == {{ condition.id }};
        }
    };
}

#endif
