/**
 * @file {{ condition.cpp_type() }}.hpp
 * @brief Generated condition.
 *
 * Generated by Eliot.
 *
 * Condition: {{ condition.cpp_type() }}
 * Purpose: Activation based on evaluation count.
 *
 * This file was automatically generated.
 */

#ifndef {{ condition.hpp_define() }}
#define {{ condition.hpp_define() }}

#include "../Packet.hpp"

namespace eliot_generated {

    /**
     * @brief Counter-based condition.
     *
     * This condition becomes fulfilled after a specified number of
     * packet evaluations. 
    {% if condition.duration is not none %}
     * It remains active only for a limited duration (number of subsequent evaluations).
    {% endif %} 
     */
    struct {{ condition.cpp_type() }} {

        /**
         * @brief Evaluates whether the condition is fulfilled.
         *
         * The condition activates after {{ condition.after }} packet evaluations.
        {% if condition.duration is not none %}
         * The condition remains active only for {{ condition.duration }} subsequent evaluations.
        {% endif %}
         *
         * @param packet Pointer to the evaluated packet (unused).
         * @return true if the condition is satisfied.
         */
        inline bool fulfilled(const Packet* /*packet*/) noexcept {
            {% if condition.duration is none %}
            return ++_counter > _AFTER;
            {% else %}
            return  ++_counter > _AFTER && _counter <= _AFTER + _DURATION;
            {% endif %}
        }

        private:

            ///> Packet evaluation counter.
            size_t _counter = 0;

            ///> Condition after parameter.
            static constexpr int _AFTER = {{ condition.after }};
            {% if condition.duration is not none %}

            ///> Condition after parameter.
            static constexpr int _DURATION = {{ condition.duration }};
            {% endif %}
    };
}

#endif
