/**
 * @file {{ generator.cpp_type() }}.hpp
 * @brief Generated value generator.
 *
 * Generated by Eliot.
 *
 * Generator: {{ generator.cpp_type() }}
 * Purpose: Provides uniformly distributed integer values.
 *
 * This file was automatically generated.
 */

#ifndef {{ generator.hpp_define() }}
#define {{ generator.hpp_define() }}

#include "../static/xoroshiro128plus.hpp"

namespace eliot_generated {

    /**
     * @brief Uniform integer value generator.
     *
     * Generates integer values uniformly distributed over a discrete interval
     * defined by the configured minimum and maximum bounds.
     *
    {% if generator.once %}
     * The generator always returns a fixed constant value.
    {% else %}
     * The generator returns dynamically generated values.
    {% endif %}
     */
    struct {{ generator.cpp_type() }} {

        /**
         * @brief Returns a generated integer value.
         *
        {% if generator.once %}
         * This method always returns the same value.
        {% else %}
         * Values are generated using a pseudo-random
         * number generator and uniformly mapped
         * to the configured integer interval.
        {% endif %}
         *
         * @return Generated integer value.
         */
        static {{ 'constexpr ' if generator.once else '' }}inline int get() noexcept {
            {% if generator.once %}
            return {{ generator.value() }};
            {% else %}
            return _generate();
            {% endif %}
        }

        {% if not generator.once %}
        private:

            ///> Minimal possible value the generator can generate.
            static constexpr int _MIN = {{ generator.min }};

            ///> Maximal possible value the generator can generate.
            static constexpr int _MAX = {{ generator.max }};

            ///> Pseudo-random number generator.
            inline static xoroshiro128plus _rng{ {{ generator.seed_value() }} };

            /**
             * @brief Generates a uniformly distributed integer value.
             *
             * The generated value lies within the closed interval [_MIN, _MAX].
             *
             * @return Uniformly distributed integer value.
             */
            inline static int _generate() noexcept {
                uint64_t value = _rng.next_range(uint64_t(_MAX - _MIN + 1));
                return static_cast<int>(value + _MIN);
            }
        {% endif %}
    };
}

#endif
