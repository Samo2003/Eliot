/**
 * @file {{ action.cpp_type }}.hpp
 * @brief Generated action.
 *
 * Generated by Eliot.
 *
 * Action: {{ action.cpp_type }}
 * Purpose: Triggers a state transition in the {{ action.state_cpp_type }} node.
 *
 * This file was automatically generated.
 */

#ifndef {{ action.hpp_define }}
#define {{ action.hpp_define }}

#include "../Packet.hpp"
#include "../states/{{ action.state_cpp_type }}.hpp"

namespace eliot::generated {

/**
 * @brief Fault injection action performing a state transition.
 *
 * This action does not modify the packet payload directly.
 * Instead, it updates the state of a referenced {{ action.state_cpp_type }} node.
 */
class {{ action.cpp_type }} {
public:
    /**
     * @brief Constructs the action with a reference to a {{ action.state_cpp_type }} node.
     *
     * @param node Reference to the node whose state will be modified.
     */
    explicit {{ action.cpp_type }}({{ action.state_cpp_type }}& node) : _node(node) {}

    /**
     * @brief Executes the state transition.
     *
     * Sets the state of the referenced node to State::{{ action.state }}.
     * The packet itself is not modified.
     *
     * @param packet Pointer to the processed packet (unused).
     */
    inline void execute(Packet* /*packet*/) noexcept {
        _node.set_state({{ action.state_cpp_type }}::State::{{ action.state }});
    }

private:
    ///> Reference to the {{ action.state_cpp_type }} node.
    {{ action.state_cpp_type }}& _node;
};

}   // namespace eliot::generated

#endif
