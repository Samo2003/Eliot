# Duplicates all UDP packets

root:
  condition:
    type: Protocol
    id: 17    # UDP
  if_true:
    action:
      type: Replicate
      n: 1
    next:
      action: { type: Finish }
  if_false:
    action: { type: Finish }
  