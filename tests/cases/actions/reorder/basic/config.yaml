name: Basic reorder

build:
  defaultAction: Finish
  rules:
    - type: all
      guards: []
      actions:
        - actionType: Reorder
          n: 10
          strategy: reverse

send:
  - count: 10
  