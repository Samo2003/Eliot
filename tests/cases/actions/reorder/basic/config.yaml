name: Basic reorder

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Reorder
          n: 10
          strategy: reverse

send:
  - count: 10
  