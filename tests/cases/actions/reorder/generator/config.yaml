name: Basic reorder

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Reorder
          n: 
            type: SeqCountInt
            min: 2
            step: 1
          strategy: reverse

send:
  - count: 10
  