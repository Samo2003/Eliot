name: Last Bitnoise test

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - actionType: BitNoise
          n: 3
          mode: last
          end: -32    # Skip seq in payload (last 4 bytes)

send:
  - count: 1
  