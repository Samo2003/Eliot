name: IP Bitnoise test

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - actionType: BitNoise
          n: 2
          mode: last
          layer: IP

send:
  - count: 1
  - src: ::1
  