name: Negative start offset Bitnoise test

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: BitNoise
          n: 2
          mode: first
          start: -34

send:
  - count: 1
  