name: Basic Bitnoise test

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: BitNoise
          n: 5
          mode: first

send:
  - count: 1
  