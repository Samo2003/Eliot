name: X exponential Bitnoise test

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: BitNoise
          x: 
            type: Exponential
            max: 0.8
            mean: 0.2
            seed: 123456
          mode: first

send:
  - count: 100
  