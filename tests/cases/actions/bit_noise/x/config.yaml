name: X Bitnoise test

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - actionType: BitNoise
          x: 0.25
          mode: first

send:
  - count: 1
  