name: Generator replicate

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Replicate
          n:
            type: SeqCount
            min: 1
            step: 10

send:
  - count: 2
  