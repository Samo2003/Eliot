name: Double replicate

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Replicate
          n: 10
        - type: Replicate
          n: 10

send:
  - count: 2
  