name: Double replicate

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Replicate
          n: 5
        - type: Replicate
          n: 5

send:
  - count: 2
  