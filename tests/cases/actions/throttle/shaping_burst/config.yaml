name: Throttle shaping burst

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Throttle
          rate: 1000
          burst: 600

timeout: 3

send:
  - count: 20
    payload_size: 100
