name: Throttle policing basic

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: []
      actions:
        - type: Throttle
          mode: policing
          rate: 1000

send:
  - count: 20
    payload_size: 100
    interval: 60
