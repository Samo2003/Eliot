name: Throttle basic shaping linear no burst

build:
  defaultAction: Finish
  rules:
    - type: all
      guards: []
      actions:
        - actionType: Throttle
          rate: 1000
          burst: 0

timeout: 3

send:
  - count: 20
    payload_size: 100
