name: Throttle policing spaced

build:
  defaultAction: Finish
  rules:
    - type: all
      guards: []
      actions:
        - actionType: Throttle
          mode: policing
          rate: 1000

timeout: 3.0

send:
  - count: 10
    payload_size: 100
    interval: 130
