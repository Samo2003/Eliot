name: Unit conversion test

build:
  defaultAction: Finish
  rules:
    - type: all
      guards: []
      actions:
        - actionType: Delay
          n: 1
          unit: s

send:
  - count: 10
  