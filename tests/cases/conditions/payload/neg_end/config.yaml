name: Payload negative end

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions:
        - type: Payload
          pattern: aaaa
          end: -6   # +4 for seq number in payload
      actions:
        - type: Finish

send:
  - payload:
      value: aaaaXX
  - payload:
      value: aaaa
  - payload:
      value: xxaaaa