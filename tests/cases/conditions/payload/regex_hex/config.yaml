name: Payload regex binary

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions:
        - type: Payload
          pattern: "\xAA\xBB.*\xCC"
          pattern_type: regex
      actions:
        - type: Finish

send:
  - payload:
      value: aabbcc
      encoding: hex
  - payload:
      value: aabb00cc
      encoding: hex
  - payload:
      value: aacc
      encoding: hex
  - payload:
      value: bbcc
      encoding: hex
