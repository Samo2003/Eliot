name: Basic IPVersion test

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - type: IPVersion
          v: 4
      actions:
        - type: Finish

send:
  - count: 1
  - src: ::1
  - dst: ::1
