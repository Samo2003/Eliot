name: Basic prob condition test

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - type: Prob
          x: 0.5
          seed: 123456
      actions:
        - type: Finish

send:
  - count: 100