name: Prob test with 1 Prob

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - conditionType: Prob
          x: 1
      actions:
        - actionType: Finish

send:
  - count: 1000