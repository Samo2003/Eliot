name: Basic protocol test

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - type: Protocol
          id: 6
      actions:
        - type: Finish

send:
  - protocol: tcp
  - src: ::1
    protocol: tcp
  - protocol: udp
  - protocol: udp
    src: ::1
