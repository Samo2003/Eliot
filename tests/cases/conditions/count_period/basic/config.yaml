name: Basic count period

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: 
        - conditionType: CountPeriod
          t: 3
      actions:
        - actionType: Drop

send: 
  - count: 12
  