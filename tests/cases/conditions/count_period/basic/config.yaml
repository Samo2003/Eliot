name: Basic count period

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: 
        - type: CountPeriod
          t: 3
      actions:
        - type: Drop

send: 
  - count: 12
  