name: Count after 0

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - conditionType: Count
          after: 0
          duration: 2
      actions:
        - actionType: Finish

send: 
  - count: 12
  