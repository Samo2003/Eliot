name: Count after 0

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - type: Count
          after: 0
          duration: 2
      actions:
        - type: Finish

send: 
  - count: 12
  