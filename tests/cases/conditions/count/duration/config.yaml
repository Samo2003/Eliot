name: Count duration

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - conditionType: Count
          after: 4
          duration: 2
      actions:
        - actionType: Finish

send: 
  - count: 12
  