name: Count duration

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - type: Count
          after: 4
          duration: 2
      actions:
        - type: Finish

send: 
  - count: 12
  