name: Basic every n

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: 
        - conditionType: EveryN
          N: 3
      actions:
        - actionType: Drop

send: 
  - count: 12
  