name: Basic every n

build:
  defaultAction: Finish
  rules:
    - type: all
      guards: 
        - guardType: EveryN
          N: 3
      actions:
        - actionType: Drop

send: 
  - count: 12
  