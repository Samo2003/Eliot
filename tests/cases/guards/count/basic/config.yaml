name: Basic count

build:
  defaultAction: Finish
  rules:
    - type: all
      conditions: 
        - conditionType: Count
          after: 4
      actions:
        - actionType: Drop

send: 
  - count: 12
  