name: Basic prob condition test

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - conditionType: Prob
          x: 0.5
      actions:
        - actionType: Finish

send:
  - count: 1000