name: Basic prob guard test

build:
  defaultAction: Drop
  rules:
    - type: all
      guards: 
        - guardType: Prob
          x: 0.5
      actions:
        - actionType: Finish

send:
  - count: 1000