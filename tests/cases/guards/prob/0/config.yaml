name: Prob test with 0 Prob

build:
  defaultAction: Drop
  rules:
    - type: all
      conditions: 
        - conditionType: Prob
          x: 0
      actions:
        - actionType: Finish

send:
  - count: 1000