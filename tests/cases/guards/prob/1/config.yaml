name: Prob test with 1 Prob

build:
  defaultAction: Drop
  rules:
    - type: all
      guards: 
        - guardType: Prob
          x: 1
      actions:
        - actionType: Finish

send:
  - count: 1000