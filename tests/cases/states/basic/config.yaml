name: Basic StateNode Test

build:
  root:
    id: Node
    initial: init
    transitions:
      INIT:
        next: 
          action: 
            actionType: ChangeState
            target: Node
            state: end
          next:
            action: 
              actionType: Finish
      end:
        next:
          action: 
            actionType: Drop

send:
  - count: 5