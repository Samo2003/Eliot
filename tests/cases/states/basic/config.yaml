name: Basic StateNode Test

build:
  root:
    id: Node
    initial: init
    transitions:
      INIT:
        action: 
          type: SetState
          target: Node
          state: end
        next:
          action: 
            type: Finish
      end:
        action: 
          type: Drop

send:
  - count: 5