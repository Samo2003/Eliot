[build-system]
requires = ["setuptools>=82", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eliot"
version = "1.0.0"
description = "Generate packet-processing pipelines from DAG specifications"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "scapy",
    "pyyaml",
    "jinja2",
    "click"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-xdist",
    "mypy",
    "types-PyYAML"
]

[project.scripts]
eliot = "eliot.cli:main"

[tool.setuptools.package-data]
eliot = ["py.typed", "templates/**/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "--ignore=tests/output"

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src", "tests", "examples"]
exclude = "tests/cases/"
install_types = true
